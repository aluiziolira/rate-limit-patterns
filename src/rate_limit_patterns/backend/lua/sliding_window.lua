-- Sliding Window Counter Rate Limiter (Placeholder)
-- KEYS[1] = rate limit key
-- ARGV[1] = limit (max requests per window)
-- ARGV[2] = period_seconds
-- ARGV[3] = current_time

-- TODO: Implement full sliding window logic in Phase 4.4
-- This is a stub script that returns deterministic values for backend loading.

-- Return: allowed, remaining, retry_after, reset_at
return {1, 99, 0, 0}
